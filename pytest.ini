[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = --ignore=temp/ --ignore=demo.py
markers =
    gradle: tests that run real Gradle builds (slow, need JDK + network)
